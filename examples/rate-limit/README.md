# 限流

- [`concurrency`](./concurrency/) 用有界 semaphore 限制同时执行的任务数量。
- [`x-time`](./x-time/) 使用令牌桶控制一段时间内的平均速率和突发量。
- [`uber`](./uber/) 使用漏桶风格限流器平滑调用间隔。

并发限制和速率限制解决不同问题，实际服务常同时使用。示例中的非阻塞 semaphore 会拒绝超额任务；需要排队时可改为阻塞获取，并结合 context 控制等待期限。
