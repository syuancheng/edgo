# 值语义与共享数据

Go 中的赋值和参数传递都会复制值。需要进一步判断的是：被复制的值内部是否包含指向共享数据的指针。

- 数组和只含值字段的结构体：复制全部数据。
- 指针：复制地址，仍指向同一对象。
- slice：复制指针、长度和容量，通常共享底层数组。
- map、channel、function：复制描述符，仍关联同一运行时对象或环境。
- interface：复制动态类型和值；动态值本身仍遵循其类型的复制语义。

需要独立所有权时执行深拷贝，不要把“传值”误解为“所有底层数据都独立”。
