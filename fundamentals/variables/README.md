# 变量、常量与零值

变量声明后会被初始化为类型零值，例如数字为 `0`、布尔值为 `false`、字符串为 `""`，指针、slice、map、channel、function 和 interface 的零值为 `nil`。

函数内部优先在类型清晰时使用 `:=`；需要强调类型或使用零值时使用 `var`。常量在编译期求值，无类型常量可在使用时获得合适类型，但转换仍必须可表示且不会绕过溢出规则。
