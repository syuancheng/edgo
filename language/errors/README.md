# 错误处理

Go 使用普通的 `error` 值表示预期失败。通常在当前层无法完整处理时返回错误，并使用 `%w` 包装底层错误以补充上下文。

- 使用 `errors.Is` 判断错误链中是否包含某个目标错误。
- 使用 `errors.As` 提取错误链中的具体错误类型。
- 不要通过比较错误字符串判断类型。
- 只在当前层能够采取恢复、降级、记录或转换行为时处理错误。
- `panic` 适用于程序不变量被破坏等不可恢复情况，不应代替普通错误返回。
